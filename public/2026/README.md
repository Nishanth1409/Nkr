# 2026 media

Drop launch → success reels here, then update `P26_MEDIA_SLOTS` in
`src/components/portfolio2026/data.ts`:

```ts
{
  id: 'm-launch',
  label: 'Launch reel',
  type: 'video',
  videoSrc: '/2026/launch.mp4',
  posterHint: 'Field light → first UI',
}
```

Keep files compressed (WebM/MP4). Do not commit multi‑GB masters.
